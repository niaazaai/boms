# Designs (Figma)

Visual UI designs will live here.

**Next step:** connect Figma MCP / create file from wireframes in `specs/wireframes/`.

Wireframes already define:
- screen layouts (mobile-first)
- page order per module
- mermaid flows in `specs/flows/`

Until Figma is linked, treat markdown wireframes as the design source of truth for structure; Figma will own visual polish (type, color, motion).
